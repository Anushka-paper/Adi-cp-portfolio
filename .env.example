# Postgres connection string (Neon or Supabase)
DATABASE_URL=

# Platform handles to sync
CODEFORCES_HANDLE=
LEETCODE_HANDLE=

# Shared secret checked against the Authorization header on /api/sync,
# so only Vercel Cron (or you, manually) can trigger a sync.
CRON_SECRET=
